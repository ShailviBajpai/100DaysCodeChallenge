TEST CASE 1:
The first and foremost possible input is that the array is possible input of strings.

TEST CASE 2:
The first and last string can be null.
so we can simply count the number of words in between.

TEST CASE 3:
If the array is in form of numbers or charecters(including special charecters)
