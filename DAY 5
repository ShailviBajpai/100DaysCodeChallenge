QUESTION: https://leetcode.com/problems/remove-duplicates-from-sorted-array/submissions/812930710/

SOLUTION: class Solution {
    public int removeDuplicates(int[] nums)
     {
       int c=0;
       for(int i=0; i<nums.length; i++)
       {
           if(nums[c]!= nums[i])
           {
               nums[++c]= nums[i];
           }
       }
       return c+1;
    }
}
